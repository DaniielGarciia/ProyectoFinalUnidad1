Bueno, el inicio del desarrollo de la página web cuenta con el menú donde se tiene el nombre de la tienda de música, además del inicio y la página 2, donde la dejé así para cumplir con lo que se solicita.

Se tienen los links para la información que se usó de Bootstrap y los estilos de CSS, donde el index es (inicioStyle.css) y la página es (paginaStyle.css).

Bajo un href="" se enlaza con el index de cada página como inicio (index.html) y página.html (pagina.html).

En el cuerpo de la página (body):

Con el nav tomamos de Bootstrap el diseño de la barra y lo modificamos a necesidad, como el uso del botón para el nombre con dirección al index. Bajo una lista (ul) usamos la etiqueta (a) para poner la dirección de las páginas o página web que se desee concatenar al darle a este botón, donde seleccionamos lo antes mencionado.

Usamos un (hr) para poner una línea en el menú.

Usamos una base sencilla de carrusel con algunas clases de Bootstrap para aprovechar la librería usada. Ponemos la información de las imágenes guardadas en la carpeta imágenes y con el (alt) definimos qué dirá en caso tal de que la imagen no funcione. Seleccionamos una etiqueta de texto (h5) para la bienvenida de la página, además de un texto o párrafo (p).

Selección de las imágenes del carrusel y la denominada alt o información de ellas.
Por último, están los botones para avanzar o retroceder en las imágenes del carrusel.

Un br para un salto de línea e iniciamos con las section o secciones donde se va desarrollando según el prototipo dado.

Hacemos un contenedor, las clases y aplicamos la información, y usamos las etiquetas para los textos.

Las img para seleccionar las imágenes con nombre de info y demás cosas que se requieren, y se repiten en cada momento.

En la siguiente sección usamos un título y aplicamos un (a) para que el botón redirija a la dirección seleccionada.

Usamos listas de nuevo con los mismos para que estas los lleven a los instrumentos seleccionados, que luego serían sacados de la tienda propiamente.

Seguimos con lo mismo de antes usando listas y demás para terminar los prototipos.

En la última sección usamos videos de YouTube con iframe para reproducirlos directamente desde nuestra página.

Los scripts de Bootstrap, y ya el footer con los div para las 3 secciones: el primero con la información, el segundo con los mismos recursos para los logos y el tercero aún sin los links del (li><a href="#" class="text-white) para cuando se lleve a esta información de la página.

Y termina el footer o pie de página.

En los styles están las clases usadas para modificar los colores y demás necesidades.

En la pagina.html se reutiliza todo el código y lo único que cambia son los 2 cajones donde se enseñan los tipos de instrumentos o sus familias. Con section se usan celdas y se entrega la información del texto con imagen, que están guardadas en la carpeta (esta acción 4 veces).

Luego se usa la opción de texto y video donde uso los títulos y los videos de lo antes mencionado. Como ya había puesto texto, omití el texto al lado del video, pero de ser necesario se agrega en la línea 91 un <p>lorem (el texto necesario y así igual en los demás)</p>.

El footer es igual y lo último es lo mismo que en la página index, entonces los dejé igual.